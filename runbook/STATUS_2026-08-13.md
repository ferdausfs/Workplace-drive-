# STATUS ADDENDUM — 2026-08-13

Read `MASTER_RUNBOOK_2026-08-08.md` for operating mindset. This file replaces **stale HEADs / next-steps** from that runbook.

---

## Live map (GitHub, 2026-08-13)

| Piece | Repo HEAD | Version in tree | Action deploy | Last archived `/health` |
|---|---|---|---|---|
| Worker | `cd3dc08` | **v6.10.1** | **red** (wrangler-action) | 08-12 05:26Z = **v6.10.0** |
| Bot | `2555d20` | **v4.5.0** | **red** | not in archive |
| App | `af9bf22` | modular + APP-001 | Vercel (not hit) | — |
| Zakat | `dca5ca8` | FTT theme | Firebase (not hit) | — |

This environment cannot TLS to `*.workers.dev` / Vercel / Firebase. **Do not quote a live version from this session.**

---

## Phase F (replay, not a new day)

Pooled 08-01..12: **WR 43.5% (n=3883, CI 42.0–45.1)**. Gate fail. FOREX 32.6% both directions. Post-v6.10 42.6% (n=336). entryHit tautology dead, no edge.

Full writeup: `reports/PHASE_F_2026-08-13.md`.

---

## Housekeeping

1. Close worker PR **#18** (dirty, superseded by #19).
2. If Telegram auto-push is still silent: check live `/health.push.tokenValid` after a real v6.10.1 deploy — that is the whole point of 6.10.1.
3. Keep RULE 6: no inversion / pair-block / real-money until CI-low ≥ 55.6%.
4. Drive snapshots now match the GitHub HEADs above. Refresh them again after the next merge.

---

## Test commands (drive checkout)

```bash
tar -xzf data/phase_f_forward_FULL_2026-08-12.tar.gz
python3 scripts/full_forward_analysis.py
python3 scripts/entryhit_corrected_analysis.py

cd worker && node scripts/fix_tests.mjs          # 304/0
cd ../bot && node single-source-test.mjs         # 72/0
```

`r71_tests` only in a real `Ftt-Otc-v6` clone (needs git history).
