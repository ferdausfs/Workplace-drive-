# BOT v4.4.2 — MERGE + DEPLOY + LIVE VERIFICATION (2026-08-09)

**Repo:** `ferdausfs/ftt-telegram-bot` · **Main:** `8a65c5c` (Merge PR #5) · **Live:** v4.4.2 deployed

---

## ✅ 1. MERGE VERIFIED
- Main = `8a65c5c` "Merge PR #5" — merged tree **byte-identical** to verified PR head `b760d01`
- BUG-B3 (duplicate signal) + BUG-B4 (1042 permanent) both merged

## ✅ 2. DEPLOY VERIFIED
- Bundle `bot-v442-20260808.js` (93,158 B, MD5 `94f3f5a9...`, v4.4.2) — built from merged main, esbuild, syntax OK
- Cloudflare API deploy: **`success: True`**, id `ftt-telegram-bot`
- Metadata included: `SIGNAL_WORKER` binding → fttotcv6 + `compatibility_flags: ["global_fetch_strictly_public"]` + `BOT_KV` + `*/5` cron

## ✅ 3. LIVE CHECKS (mine)
```
bot root:      FTT Signal Bot v4.4.2   ← new version live
bot HTTP:      200
worker health: healthy (fttotcv6, quota ok)
```

## ✅ 4. CODE-LEVEL DUPLICATE FIX CONFIRMED
- `autoScan` (cron) no longer calls `sendMsg(cid, fmtSignal(...))` — verified in source + in the deployed bundle (only manual triggers `doSignal`/`doQuickSignal`/`doScanAll` still send, which is correct user-initiated behavior)
- Worker push (`*/2` cron) is the single auto-delivery path — has `claimPushLock` + pushLog dedup

## ✅ 5. 1042 PERMANENT
- `wrangler.toml` carries `compatibility_flags = ["global_fetch_strictly_public"]` → future GitHub-Actions deploys won't regress
- Runtime deploy also has the flag + binding (both paths correct)

---

## 🧪 USER TEST (the only thing I can't do from here)
In Telegram:
1. **Start Auto ON** (if not) → wait for next signal → **confirm you receive exactly ONE message** (worker push), not two.
2. `/signal` manual → still works.
3. `/alerts` → should show "unknown command" style (removed — expected).
4. `/history`, `/stats` → still work.

If anything's off, screenshot/message me.

## STATUS SUMMARY (all three components)
| Component | Main HEAD | Live | Deploy path |
|---|---|---|---|
| Worker | `7ed962a` | ✅ FIX-EH bundle live | manual bundle (`worker-fixeh-20260807.js`) |
| App | `3d2e876` | ✅ live | Vercel auto (PR #4 merge = CHECK) |
| Bot | `8a65c5c` | ✅ v4.4.2 | manual bundle (`bot-v442-20260808.js`) |

## OPEN ITEMS
1. **App PR #4** (APP-001 + SignalHero) — verified, merge status = CHECK → merge + Vercel auto-deploy.
2. **Cloudflare token rotation** — `cfut_pTef5...` leaked earlier; confirm revoked/recreated.
3. **Phase F** — daily snapshot + corrected entryHit + D4 ML continue (data accumulating).
4. **Deriv demo integration** — on hold per user (demo first); needs app_id/token research completed.
