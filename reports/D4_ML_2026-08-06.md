# D4 ML — RUN 2026-08-06 (Arena main agent, independent)

**Data:** phase_f_forward 08-01..08-06 · forward decided **n=2356** (6 days) · test = 08-06 (n=220, partial pre-merge day)
**Bar:** 55.6% WR (80% payout breakeven) · chronological split · Wilson CI · xgboost 120/max_depth3/lr0.08

---

## Baseline (engine itself)
- **Engine WR: 40.7% (960/2356, CI 38.8–42.7%)** — sub-breakeven as expected.

## LEGIT model (signal-time features ONLY — entryHit excluded)
| Metric | n | WR (CI) | vs 55.6% |
|---|---|---|---|
| all-pred accuracy | 220 | 62.3% | (majority-class, predicts LOSS mostly) |
| confident-only WR | 163 | 35.0% (28.1–42.6) | ⚠️ no edge |
| **WIN-call subset p≥0.50** | 32 | **46.9% (30.9–63.6)** | ⚠️ point est. below breakeven, CI wide |
| **WIN-call subset p≥0.55** | 13 | **46.2% (23.2–70.9)** | ⚠️ no edge |

Top features: `at_FOREX` (0.062), `at_CRYPTO` (0.054), `align_ALL_BULLISH`, `p_GBP/USD`, `dir_SELL` — **the model's #1 signal is "this is forex"**, consistent with the day-2 finding (FOREX WR 16%).

**Verdict: NO EDGE.** The model cannot predict WIN at >55.6% using signal-time information. CI on every trading metric either straddles breakeven or sits below it.

## LEAKAGE diagnostic (entryHit INCLUDED — post-hoc, tautology)
| Metric | n | WR (CI) | vs 55.6% |
|---|---|---|---|
| all-pred accuracy | 220 | 90.5% | — |
| **WIN-call subset** | 60 | **100.0% (94–100)** | ✅ "beats breakeven" — FAKE |

**This is the smoking gun.** The "100% WR" subset = exactly the **60/60 miss rows** from day-2 analysis (`entryHit=false ⟹ WIN` is mathematically guaranteed with the expiry±5min window). The model didn't learn signal quality — it learned the tautology. `entryHit` importance 0.170 (top feature). This is **untradeable**: entryHit is only known AFTER the expiry window, at signal time nobody knows it.

## Honest conclusions
1. **D4 ML finds no edge** — legit model: 46.9% / 46.2% WIN-call WR, below the 55.6% bar, wide CIs.
2. **The 100% is provably fake** — same tautology we proved with the 200k-row sim, now reproduced by the model itself. Anyone reporting "entryHit-based 100% strategy" is reporting leakage.
3. **No conditional strategy from this run.** Continue collecting; rerun tomorrow with post-merge data (fixes: post-AI floor, D2 rescue guard, fillStatus, /12, grade cap — these change the signal stream).
4. entryHit remains an **analysis-only shadow metric** (BUG-004): needs the corrected [signalTime→expiry] window to ever be meaningful.

## Next run trigger
- After tomorrow's snapshot (08-07) — post-merge signal stream, n grows, train gains real entryHit days (but feature still excluded for legitimacy).
