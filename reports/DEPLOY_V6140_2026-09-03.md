# DEPLOY v6.14.0 — V7 SHADOW + SAFETY PATCH (2026-09-03)

## Status: LIVE

- Version: **6.14.0** (confirmed via /health at ~06:21 UTC)
- PR: [#32 feat/v7-shadow → main](https://github.com/ferdausfs/Ftt-Otc-v6/pull/32) — merged
- Bundle: esbuild, 348,915 bytes, deploy via CF direct-API (redeploy.sh)
- Push health at verify: tokenValid=True (fttbotbot), 1 subscriber, delivered24h=78

## What shipped

### 1. V7 next-gen engine prototype — SHADOW ONLY (zero live effect)

Paradigm change: the old vote-counting/confluence scoring is measured dead
(4-day pooled push WR 37.3%, score↔win correlation **-0.063**, best slice only
52.3%). V7 v0.1 replaces it with **router + exclusion + trigger**:

- Regime router: crypto RANGING only (TRENDING/FOREX excluded by design)
- Hard veto stack: dead-squeeze (BB width), ATR explosion, measured bad hours
  {01,02,03,11,14,19} UTC (each ≤29.5% WR, n≥16)
- H2 extremes + non-chase: BUY only at %B≤0.15 + RSI≤40; SELL at %B≥0.85 + RSI≥60
- H1 trigger: closing rejection candle in trade direction (close position ≥0.5)
- Counterfactual KV store `v7obs:` (SIGNAL_CACHE): every crypto tick (incl.
  NO_TRADE) is admitted via `waitUntil`, dedup 30min/pair, cap 40/pair/30d,
  forward-resolved with the production `fetchExpiryPrice`, 5-min binary expiry

**Flip gate (RULE-6 discipline, same gate that cancelled the EC flip):**
shadow WR ≥ 60% with n ≥ 100 and Wilson CI lower bound > 50% before any
decision-mode discussion. Breakeven at 80% payout = 55.6%.

### 2. Safety patch — LIVE EFFECT (push volume collapses on purpose)

| Change | Old | New | Evidence (4-day pool, 630 decided) |
|---|---|---|---|
| D2_TRENDING_BLOCK_ENABLED | false | **true** | TRENDING 35.4% WR n=325; TRENDING+BUY 20.5% n=39 |
| SELECTIVITY_GATE.excludeTrending | false | **true** | same slice, push layer belt-and-braces |
| SELECTIVITY_GATE.excludeChase (NEW) | — | **true** | CHASE = 73% of volume @ 37.2% WR; non-CHASE 52.3% n=44 |

Expected: ~163/day @ 37.3% → **~11/day @ ~52%** push WR. History volume is
UNCHANGED (gates control push only) — the forward research pool keeps growing.

## Tests (all green before merge)

- `selectivity_chase` **15/15** (new: boundaries 55/45, fail-open, kill-switch, ordering)
- fix 328/328 · rsi_gate 18/18 · ec_v2 43/43 · v7 13/13

## Rollback (one-line each, no redeploy of branch logic)

- TRENDING block: `D2_TRENDING_BLOCK_ENABLED: false`
- Chase gate: `SELECTIVITY_GATE.excludeChase: false`
- V7 shadow: `V7_SHADOW.enabled: false`

## Next

1. Let V7 shadow accumulate ≥5–7 days of forward data (~day: 10-pair crypto
   ticks, dedup-capped).
2. Weekly summary from v7obs slices; compare vs the 52.3% non-CHASE baseline.
3. If flip gate passes → discuss decision mode + threshold loosening (volume).
4. If it fails → thresholds re-derived from the store's own slices, or v0.2
   adds FXUSD/OTC regimes.
