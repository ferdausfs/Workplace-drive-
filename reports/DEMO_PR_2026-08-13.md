# Demo pull request — 2026-08-13

This documentation-only change verifies the repository's branch → commit → push
→ pull-request workflow without changing application behavior.

## Validation target

The Android release validation command remains:

```bash
cd android
./gradlew assembleRelease
```

After this demo PR is reviewed and merged or closed, the same session branch can
be updated and used to open the next PR.
