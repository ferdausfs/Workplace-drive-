# FTT3 DEPLOY — 2026-09-05

**Fresh engine live on Ftt-Otc-v6 main @ 2df8f54** (full replacement; v1/v6/v2
logic deleted from the tree, history preserved in git + `engine-v2` branch).

## What shipped

- **FTT3-v1.0.0**: 3 conditions, 3 timeframes, nothing else —
  C1 EMA20/50 bias (15m, last closed) → C2 MACD(12,26,9) cross (5m, last
  closed, direction-aligned) → C3 ATR(14)@1m ≥ trailing-100 median.
  Dynamic expiry at entry: ATR-pct ≥75 → 5m · 25–75 → 7m · <25 → 10m
  (tiers frozen before any backtest).
- **Walk-forward backtest** (split `2026-08-16T00:00Z` committed pre-run,
  OOS touched once): **FAIL — 50.5% WR [46.6, 54.3] vs 55.6% breakeven @ 0.80
  payout.** 746 OOS signals over 8 pairs; baseline up-rate 47.4–48.2%.
  No rescue filter added; full report + 37,958-row audit JSONL in the repo
  (`results/`), every number re-derived by `scripts/verify_audit.mjs`.
- **Live plumbing kept/rebuilt**: TwelveData fetch + 19-key rotation, KV
  caches, Telegram push (no grade/conf filters — FTT3 has none), 30-min
  dedup, */5 scanner aligned to 5m closes with boundary-lag retries
  (cache-bypassing), */2 result checker (WIN/LOSS/TIE/EXPIRY_GAP).
  OTC pairs removed entirely.
- **Tests**: 42 strategy assertions (incl. no-lookahead mutation proof) +
  33 smoke checks on the stubbed live path + 72 audit-verification checks.
  Smoke caught a real production bug pre-deploy (exit-price field mismatch).

## Deploy trail

1bddb43 split commit → a4d3cb5 OOS results → 48c6c6e engine swap →
b4ad902 README → 3cd359d (broken, CI caught) → db7564c fixed →
2df8f54 lag-retry cache fix. CI = GitHub Actions auto-deploy (rotated
CLOUDFLARE_API_TOKEN secret), all green. `/health` = FTT3-v1.0.0 healthy
with the FAIL verdict printed.

## Next

- Let FTT3 collect forward data; per-strategy-style verdicts come from
  `/api/history` + `/api/stats` (CALL/PUT rows with full indicator audits).
- Pre-registered hypotheses for a FUTURE fresh-data walk-forward only:
  USDJPY strength, 7m>5m tier, forex>crypto — do NOT bolt onto C1–C3.
- AUD/USD needs a clean 1m source before any conclusion.
- Rotate/revoke the session tokens (standing reminder).
